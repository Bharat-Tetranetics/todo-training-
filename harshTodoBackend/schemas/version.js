//versions format
/*
versions={
   v1:[{
      query:"alter table app add username varchar(255))",
      description:"adding a new column username"
      tableName:"app"
      },
      {
      query:"Alter table newApp(id int primary key,name varchar(255)",
      description:"adding username to table newApp"
      tableName:"newApp"
      }],
}
*/
//write the versions below
const versions = {};

module.exports = versions;
